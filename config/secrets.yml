development:
  secret_key_base: <%= ENV.fetch("SECRET_KEY_BASE", "dev-secret-key-change-me") %>

test:
  secret_key_base: <%= ENV.fetch("SECRET_KEY_BASE", "test-secret-key-change-me") %>

production:
  secret_key_base: <%= ENV.fetch("SECRET_KEY_BASE") %>

staging:
  secret_key_base: <%= ENV.fetch("SECRET_KEY_BASE") %>
