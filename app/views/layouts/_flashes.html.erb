<% unless flash.empty? %>
  <div id="flash">
    <% flash.each do |key, value| %>
      <div title="<%= key.to_s.humanize %>" class="<%= key %>">
        <p><%= raw value %></p>
      </div>
    <% end %>
  </div>
<% end %>
