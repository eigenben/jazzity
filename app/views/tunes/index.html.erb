<%= render "search_box" %>

<section id="tunes">
  <%= render @tunes %>
  <%= paginate @tunes %>
  <% if @tunes.empty? %>
    <p class="no-results">Sorry, but no tunes were found matching your search.</p>
  <% end %>
</section>

<div id="sidebar"></div>
